/** Nest DI token for {@link AsrAdapter}. */
export const ASR_ADAPTER = Symbol('ASR_ADAPTER');

/** Nest DI token for {@link ImageGenAdapter}. */
export const IMAGE_GEN_ADAPTER = Symbol('IMAGE_GEN_ADAPTER');
